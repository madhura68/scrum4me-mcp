node_modules
dist
.env
.env.local
*.log
.DS_Store

# Generated Prisma Client
src/generated
prisma/generated

# Editor
.vscode
.idea

# Claude Code worktrees (per-session, never tracked)
.claude/worktrees/

# Disposable, digest-verified build toolchains (IDEA-187 companion release).
# Never staged, never manifested, never copied into a release tree.
.artifacts/

# Release attestation output (IDEA-187): build metadata + gate evidence,
# regenerated from the exact origin/main merge commit — never committed.
.release/
